// About Section Controller
export function initAbout() {
  // Can add count-up animation for stat cards if desired
}

if (typeof document !== 'undefined') {
  document.addEventListener('DOMContentLoaded', () => {
    initAbout();
  });
}
