// Testimonials Section Controller
export function initTestimonials() {}

if (typeof document !== 'undefined') {
  document.addEventListener('DOMContentLoaded', () => {
    initTestimonials();
  });
}
